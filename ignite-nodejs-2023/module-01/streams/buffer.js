const buf = Buffer.from("adriano luis de almeida")

console.log(buf.toJSON())


//é uma representação de um espaço em memória para ser trafegado na aplicação
//modo de ler em modo binário = muito mais performático

